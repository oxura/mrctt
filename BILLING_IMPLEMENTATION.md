# Реализация Биллинга и Подписок

## Обзор

Добавлена полная система биллинга и управления подписками для CRM платформы. Реализация включает:

- Таблицы базы данных для подписок и платежей
- API endpoints для управления подписками
- UI страницы для просмотра биллинга и выбора тарифов
- Заглушку платежного шлюза для MVP
- Ручное продление подписки для тестирования

## Структура БД

### Таблица `subscription_plans`
Предопределенные тарифные планы:
- `free` - Бесплатный ($0/месяц)
- `starter` - Стартовый ($29/месяц)
- `pro` - Профессиональный ($99/месяц)
- `enterprise` - Корпоративный ($299/месяц)

Поля:
- `id` (VARCHAR) - Уникальный идентификатор плана
- `name` - Название плана
- `description` - Описание
- `price` (DECIMAL) - Цена
- `currency` - Валюта
- `billing_period` - Период ('monthly' | 'yearly')
- `features` (JSONB) - Массив возможностей
- `is_active` - Активность плана

### Таблица `subscriptions`
Подписки арендаторов:
- `id` (UUID) - Уникальный ID
- `tenant_id` (UUID) - Ссылка на компанию
- `plan_id` - Ссылка на тарифный план
- `status` - Статус ('trial' | 'active' | 'cancelled' | 'expired' | 'past_due')
- `current_period_start` - Начало текущего периода
- `current_period_end` - Конец текущего периода
- `trial_ends_at` - Дата окончания пробного периода
- `cancelled_at` - Дата отмены

### Таблица `payments`
История платежей:
- `id` (UUID) - Уникальный ID
- `tenant_id` - Ссылка на компанию
- `subscription_id` - Ссылка на подписку
- `amount` (DECIMAL) - Сумма платежа
- `currency` - Валюта
- `status` - Статус ('pending' | 'completed' | 'failed' | 'refunded')
- `payment_method` - Метод оплаты
- `transaction_id` - ID транзакции
- `invoice_url` - Ссылка на счет
- `description` - Описание
- `paid_at` - Дата оплаты

## Backend API

### Endpoints

#### `GET /api/v1/billing`
Получить обзор биллинга (требует роль `owner`)

**Response:**
```json
{
  "subscription": {
    "id": "uuid",
    "tenant_id": "uuid",
    "plan_id": "pro",
    "status": "active",
    "current_period_start": "2024-01-01T00:00:00Z",
    "current_period_end": "2024-02-01T00:00:00Z",
    "plan": {
      "id": "pro",
      "name": "Pro",
      "price": "99.00",
      "features": ["..."]
    }
  },
  "payments": [...],
  "plans": [...]
}
```

#### `POST /api/v1/billing/change-plan`
Изменить тарифный план (требует роль `owner`)

**Request:**
```json
{
  "planId": "pro"
}
```

#### `POST /api/v1/billing/renew`
Продлить подписку вручную (требует роль `owner`)

**Request:**
```json
{
  "months": 1
}
```

#### `POST /api/v1/billing/payments`
Создать тестовый платеж (требует роль `owner`)

**Request:**
```json
{
  "planId": "pro",
  "amount": 99.00,
  "paymentMethod": "test_payment"
}
```

## Frontend

### Страницы

#### `/billing` - Страница биллинга
Отображает:
- Текущую подписку с деталями (план, статус, дата окончания)
- Функции выбранного плана
- Кнопки "Изменить тариф" и "Продлить на месяц"
- Историю платежей в таблице

Доступ: только для `owner`

#### `/billing/plans` - Выбор тарифного плана
Отображает:
- Карточки всех доступных тарифных планов
- Сравнение возможностей
- Модальное окно подтверждения оплаты
- Тестовую заглушку платежного шлюза

Доступ: только для `owner`

### Навигация
Пункт меню "💳 Биллинг" добавлен в sidebar, отображается только для пользователей с ролью `owner`.

## Миграции

Миграция: `00021_subscriptions_and_payments.sql`

**Запуск миграции:**
```bash
cd backend
npm run migrate
```

**Откат миграции:**
```bash
cd backend
# Выполнить вручную 00021_subscriptions_and_payments.down.sql
```

## Логика работы

### Изменение плана
1. Пользователь выбирает новый план на странице `/billing/plans`
2. Подтверждает выбор в модальном окне
3. Система создает/обновляет подписку
4. Создается запись платежа с тестовым ID транзакции
5. Обновляется `subscription_status` и `subscription_expires_at` в таблице `tenants`

### Продление подписки
1. Пользователь нажимает "Продлить на месяц"
2. Система вычисляет новый период (от текущего `current_period_end` + 1 месяц)
3. Обновляет подписку и метаданные арендатора

### RLS Политики
Все таблицы используют Row Level Security для изоляции данных:
- `subscription_plans` - публичный доступ для чтения активных планов
- `subscriptions` - изоляция по `tenant_id`
- `payments` - изоляция по `tenant_id`

## Тестирование

### Создание тестовой подписки
```bash
# Через UI:
1. Войти как owner
2. Перейти в /billing
3. Нажать "Выбрать тариф"
4. Выбрать план и нажать "Оплатить"

# Через API:
curl -X POST http://localhost:5000/api/v1/billing/change-plan \
  -H "Content-Type: application/json" \
  -H "X-Tenant-ID: <tenant-id>" \
  --cookie "access_token=<token>" \
  -d '{"planId": "pro"}'
```

### Продление подписки
```bash
curl -X POST http://localhost:5000/api/v1/billing/renew \
  -H "Content-Type: application/json" \
  -H "X-Tenant-ID: <tenant-id>" \
  --cookie "access_token=<token>" \
  -d '{"months": 1}'
```

## Безопасность

- Все endpoints биллинга требуют роль `owner`
- RLS обеспечивает изоляцию данных между арендаторами
- CSRF защита для всех мутирующих операций
- Валидация всех входных данных через Zod

## TODO для Production

- [ ] Интеграция реального платежного шлюза (Stripe, PayPal и т.д.)
- [ ] Автоматическое продление подписок
- [ ] Email уведомления о статусе платежей
- [ ] Webhook обработка событий платежного шлюза
- [ ] Генерация PDF счетов
- [ ] Промо-коды и скидки
- [ ] Логика grace period для просроченных подписок
- [ ] Метрики и аналитика биллинга

## Файлы

### Backend
- `backend/migrations/00021_subscriptions_and_payments.sql` - Миграция БД
- `backend/src/types/models.ts` - TypeScript типы
- `backend/src/repositories/subscriptionRepository.ts` - Репозиторий подписок
- `backend/src/repositories/paymentRepository.ts` - Репозиторий платежей
- `backend/src/services/billingService.ts` - Бизнес-логика
- `backend/src/controllers/billingController.ts` - API контроллеры
- `backend/src/routes/billingRoutes.ts` - Маршруты

### Frontend
- `frontend/src/types/index.ts` - TypeScript типы
- `frontend/src/pages/billing/Billing.tsx` - Страница биллинга
- `frontend/src/pages/billing/Billing.module.css` - Стили
- `frontend/src/pages/billing/Plans.tsx` - Страница выбора тарифов
- `frontend/src/pages/billing/Plans.module.css` - Стили
- `frontend/src/App.tsx` - Маршруты приложения
- `frontend/src/components/navigation/Sidebar.tsx` - Навигация

## Итог

✅ Все задания выполнены:
- [x] Создать таблицу subscriptions в БД
- [x] Создать таблицу payments
- [x] Создать страницу биллинга с карточкой текущего тарифа
- [x] Отображать дату окончания подписки и статус
- [x] Создать таблицу истории платежей
- [x] Реализовать кнопку "Изменить тариф"
- [x] Создать страницу выбора тарифов
- [x] Заглушка для платежного шлюза
- [x] Реализовать ручное продление подписки
